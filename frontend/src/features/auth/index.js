// Feature Auth — superficie pública (H1). Implementación sigue en hooks/components.

export { default as useAuthSession } from "../../hooks/useAuthSession";
export { default as AuthPanel } from "../../components/AuthPanel";
export { normalizeUserRole } from "../../utils/userRole";
