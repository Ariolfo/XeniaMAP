# API HTTP image (H5). Same Python deps as full stack today (preview/MVT/SoilPlus sync).
# Heavy / native GIS extras (SNAP, GDAL build-time) MUST go in requirements-worker.txt only.
-r requirements.txt
