# Worker Celery image (H6): common + AWS SDK (Fire S2 download) + room for SNAP/GDAL.
-r requirements-common.txt
boto3==1.35.36
botocore==1.35.36
# Future worker-only pins (example):
# snapista==...
# gdal==...
